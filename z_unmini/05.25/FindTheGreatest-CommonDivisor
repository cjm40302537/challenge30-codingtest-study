//a를 입력받는다 공백을 기준으로 나눈다
//split()을 통해 만들어진 배열의 값을 0번째자리 값을 정수형으로 변환해 n에 넣는다?
//split()을 통해 만들어진 배열의 값을 1번째자리 값을 정수형으로 변환해 m에 넣는다?
//1부터 n+1 범위에서 반복한다.
//n%i==0이고 m%i==0인지 검사한다.
//참이라면 i를 c에 넣는다.
//위에 정한 범위에 반복이 끝나면 c(최대공약수)를 출력한다


a=input().split()
n=int(a[0])
m=int(a[1])
for i in range(1,n+1):
    if(n%i==0) and (m%i==0):
        c=i
print(c)
